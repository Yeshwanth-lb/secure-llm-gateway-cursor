# Node / TypeScript
node_modules/
npm-debug.log*
yarn-error.log*
*.tsbuildinfo
dist/
build/

# Config secrets / local state
config/config.local.yaml
.claude/settings.local.json
*.db
*.sqlite
*.sqlite3
gateway.db

# Env
.env
.env.*

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# OS / editor
.DS_Store
.idea/
.vscode/

# Generated PII test fixture (regenerate via scripts/gen-pii-sample.mjs) — never commit PII-shaped data
pii-sample.txt

# Admin control-plane data (Phase U) — local SQLite DB + JWT signing key, never commit
*.db
*.db-wal
*.db-shm
jwt.key

# Local screenshots + throwaway upload/PII test fixtures (regenerate as needed)
Screenshot *.png
pii-block-test.txt
upload-block-test.pdf
upload-office-test.docx
